query NoticePostNavigation(
  $noticePostNavigationId: ID!
  $params: NoticePostNavigationParamsInput
) {
  noticePostNavigation(id: $noticePostNavigationId, params: $params) {
    prePostId
    nextPostId
    params {
      search
    }
  }
}
